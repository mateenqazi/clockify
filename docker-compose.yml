version: '3'
services:
  postgres:
    image: postgres
    container_name: clockify
    ports:
      - "5434:5432"
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: clockify